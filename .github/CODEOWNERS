# This is a comment.
# Each line is a file pattern followed by one or more owners.

# These owners will be the default owners for everything in
# the repo. Unless a later match takes precedence,
# @global-owner1 and @global-owner2 will be requested for
# review when someone opens a pull request.
* @adrianeyre

# Order is important; the last matching pattern takes the most
# precedence. When someone opens a pull request that only
# modifies JS files, only @js-owner and not the global
# owner(s) will be requested for a review.
# *.js    @js-owner

# You can also use email addresses if you prefer. They'll be
# used to look up users just like we do for commit author
# emails.
# *.go docs@example.com

# Teams can be specified as code owners as well. Teams should
# be identified in the format @org/team-name. Teams must have
# explicit write access to the repository. In this example,
# the octocats team in the octo-org organization owns all .txt files.
# *.txt @octo-org/octocats

# In this example, @doctocat owns any files in the build/logs
# directory at the root of the repository and any of its
# subdirectories.
# /build/logs/ @doctocat

# The `docs/*` pattern will match files like
# `docs/getting-started.md` but not further nested files like
# `docs/build-app/troubleshooting.md`.
# docs/*  docs@example.com

# In this example, @octocat owns any file in an apps directory
# anywhere in your repository.
# apps/ @octocat

# In this example, @doctocat owns any file in the `/docs`
# directory in the root of your repository and any of its
# subdirectories.
# /docs/ @doctocat

# In this example, any change inside the `/scripts` directory
# will require approval from @doctocat or @octocat.
# /scripts/ @doctocat @octocat

# In this example, @octocat owns any file in the `/apps`
# directory in the root of your repository except for the `/apps/github`
# subdirectory, as its owners are left empty.
# /apps/ @octocat
# /apps/github

# Dependency manifest files — no code owner (blank entry overrides the catch-all above).
# GitHub: "if the last matching pattern has no owner, changes to those files do not
# require code-owner review." This makes require_code_owner_review vacuously satisfied
# for Dependabot PRs, so the github-actions[bot] approval in dependabot-automerge.yml
# only needs to satisfy required_approving_review_count:1 (ADR-0042 §2 Lane 1).
# Pattern covers all workspace manifests: root, apps/*, packages/*, plugins/*, skills/*.
**/package.json
**/pnpm-lock.yaml

# GitHub-Actions Dependabot bumps edit the `uses:` refs INSIDE workflow files, not a
# separate manifest — so the blank override has to cover the workflow files themselves,
# or a github_actions Lane-1 PR (issue #954, Track D → Lane 1) still matches the
# catch-all `* @Percayso-Community/community-engineers` and the github-actions[bot] approval cannot satisfy
# required code-owner review (Phase-2 `gh pr merge` would then be rejected by branch
# protection). This mirrors the npm-manifest exemption above: it drops only the
# *code-owner* review requirement; branch protection's required_approving_review_count:1
# still holds (satisfied by the bot for Dependabot PRs, by a reviewer for human PRs).
/.github/workflows/
